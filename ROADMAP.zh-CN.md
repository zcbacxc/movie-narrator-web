[![English](https://img.shields.io/badge/English-Roadmap-blue)](ROADMAP.md)
[![简体中文](https://img.shields.io/badge/简体中文-路线图-green)](ROADMAP.zh-CN.md)

# 路线图

> 各版本的详细说明见 [CHANGELOG.md](CHANGELOG.md)。本包使用与核心引擎
> （`movie-narrator`）**相互独立的版本线**；两者解耦，兼容性由
> `CONTRACT_VERSION` 决定。

## 已完成

| 版本 | 核心主题 |
|---------|-------------|
| v1.0.1 | 初始发布 / FastAPI + React SPA / WebSocket 进度 / REST 任务 API / PyPI 包 / 导入时契约检查 |
| v1.0.2 | 契约边界治理 / `check_version()` 工具 / 测试契约合规修复 |
| v1.0.3 | 许可证标准化 / PEP 639 / SPDX 头 / AGPL-3.0-or-later |
| v1.0.4 | 双语 i18n（中/英）/ 语言切换器 / 后端 `lang` 透传 |
| v1.1.0 | **对齐核心引擎 v1.0** / `video_format` 映射 / 最低契约 `(1, 0, 0)` / 依赖下限 `>=1.0.0` |

当前版本要求的最低契约：`(1, 0, 0)`。

---

## 当前与计划

> **规划原则**：发布增量式、低风险的小版本，始终与核心引擎的契约面保持
> 对齐。Web UI 是 `movie_narrator.contract` 的薄消费者；其路线图关注前端
> 体验与运维便捷性，而非引擎功能。

### v1.2.0 — Web UI 打磨（规划中）

> **目标**：在不改动 API 契约的前提下，提升操作体验与自助洞察能力。

- [ ] 丰富的任务详情视图 — 分步耗时、产物预览卡片
- [ ] UI 内的设置 / 环境视图（引擎配置的只读镜像）
- [ ] 重启后持久化的任务历史（简易 JSONL 存储）
- [ ] 创建面板的键盘快捷键
- [ ] 前端无障碍通过（ARIA、焦点管理）

---

### v1.3.0 — 部署与运维（规划中）

> **目标**：让 Web UI 达到单租户服务场景的生产可用。

- [ ] 通过带 `MN_WEB_` 前缀的环境变量配置绑定 host / port
- [ ] API 可选静态令牌认证
- [ ] 请求日志 + 基础请求指标端点
- [ ] 与核心引擎并列的 Web UI Docker 镜像
- [ ] 与核心引擎 `/api/health` 对齐的健康检查

---

### v1.3 之后 — 需求驱动

以下项目尚未承诺；将根据社区反馈确定优先级：

- 多用户 / 工作区 — 认证、按用户隔离任务（仅当 SaaS 需求出现时）
- 任务队列看板 — 浏览器内队列可视化与重排
- 插件管理 UI — 在浏览器中启用 / 禁用已安装的核心引擎插件
- 移动端布局优化 — 面向触控的结果与监控视图